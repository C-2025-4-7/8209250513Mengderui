/*#ifndef STUDENT_H  // 头文件保护，防止重复包含（必加，避免编译错误）
#define STUDENT_H

class Student              //类声明
{
public:                   //公用成员函数原型声明
	void display();
	void set_value(int n, const char* na, char s) {
		n = num;
		int i = 0;
		while (na[i] != '\0' && i < 19) {
		}name[i] = na[i];
		i++;
		sex = s;
	}
private:
	int num;
	char name[20];
	char sex;
	
};
#pragma once
#endif */